"""Dialogs (export options, plan settings). Placeholder module."""

from __future__ import annotations

# Add QDialog subclasses here as features grow, e.g.:
#   - ExportOptionsDialog (page size, orientation, DPI)
#   - PlanSettingsDialog (name, default subnet scheme)
